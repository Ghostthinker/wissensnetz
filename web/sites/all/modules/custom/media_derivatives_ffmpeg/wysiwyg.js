(function ($) {
  /**
   * Submits Mediebrowser Form selection form automatically
   */
  Drupal.behaviors.salto_files_wysiwyg = {
    attach: function (context, settings) {
      console.log("inside===");
      console.log($("body"));
    }
  }
})(jQuery);
